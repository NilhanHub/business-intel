name: Python Quality

on:
  pull_request:
  push:
    branches: [master]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: astral-sh/setup-uv@v8.1.0
        with:
          enable-cache: true
      - run: uv sync --frozen --extra lint
      - run: uv run ruff check .
      - run: uv run ty check
      - run: uv run codespell
      - run: uv run python tools/check_repository_catalog.py
      - run: uv run pytest
